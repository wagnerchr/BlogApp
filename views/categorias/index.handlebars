<h1>Categorias</h4>
<hr>

{{#each categorias}}
<div class="card mt-4">
    <div class="card-body">
        <a href="/categorias/{{slug}}">
            <h4>{{nome}}</h4>
        </a>
    </div>
</div>

{{else}}
    <h4>Nenhuma categoria registrada</h4>
{{/each}}